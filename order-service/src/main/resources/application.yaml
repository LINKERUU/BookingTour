server:
  port: 8085

spring:
  application:
    name: order-service
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

  mongodb:
    uri: mongodb://localhost:27017/order_db

  jwt:
    secret: ${JWT_SECRET}

